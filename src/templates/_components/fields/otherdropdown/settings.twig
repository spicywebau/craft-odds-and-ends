{% import "_includes/forms" as forms %}

{{
	forms.textField({
		label: 'Label for the other option'|t,
		name: 'otherLabel',
		id: 'otherLabel',
		value: field['otherLabel'],
		errors: field.getErrors('otherLabel')
	})
}}

{{
	forms.textField({
		label: 'Placeholder for the other field'|t,
		name: 'otherPlaceholder',
		id: 'otherPlaceholder',
		value: field['otherPlaceholder'],
		errors: field.getErrors('otherPlaceholder')
	})
}}